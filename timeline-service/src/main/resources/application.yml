server:
  port: 8083
spring:
  application:
    name: timeline-service
  redis:
    database: 0
    host: ${REDIS_URL:localhost}
    port: 6379
    password: eYVX7EwVmmxKPCDmwMtyKVge8oLd2t81
    timeout: 60000

eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:9000/eureka}

cloud:
  aws:
    stack:
      auto: false
      enabled: false
    region:
      static: us-west-1
    sqs:
      url: http://${SQS_URL:localhost}:4576

client:
  user-followers:
    url: http://${USER_URL:localhost}:8081